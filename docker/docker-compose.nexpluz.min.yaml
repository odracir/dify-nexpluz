# NEXPLUZ production compose for Dify 1.17.0
# Reduced from the upstream Dify compose for Coolify deployment.
#
# External infrastructure:
#   - PostgreSQL 18 + pgvector via DIFY_DB_HOST / DIFY_DB_PASSWORD
#   - Redis via DIFY_REDIS_HOST / DIFY_REDIS_PASSWORD
#
# Optional capability retained:
#   - api_websocket remains available under profile: collaboration
#
# Intentionally NOT bundled here:
#   internal PostgreSQL/Redis, alternate vector databases, Certbot,
#   Elasticsearch/OpenSearch/Milvus/Qdrant/Weaviate/etc.
# These can be added later from upstream without changing the core Dify data.
#
services:
  init_permissions:
    image: busybox:latest
    command:
    - sh
    - -c
    - "FLAG_FILE=\"/app/api/storage/.init_permissions\"\nif [ -f \"$${FLAG_FILE}\" ]; then\n  echo \"Permissions already initialized.\
      \ Exiting.\"\n  exit 0\nfi\necho \"Initializing permissions for /app/api/storage\"\nchown -R 1001:1001 /app/api/storage\
      \ && touch \"$${FLAG_FILE}\"\necho \"Permissions initialized. Exiting.\"\n"
    volumes:
    - ./volumes/app/storage:/app/api/storage
    restart: 'no'
  api:
    env_file:
    - path: ./envs/core-services/shared.env
      required: false
    - path: ./envs/core-services/api.env
      required: false
    - path: ./envs/security.env
      required: false
    - path: ./envs/databases/db-postgres.env
      required: false
    - path: ./envs/databases/redis.env
      required: false
    - ./.env
    networks:
    - ssrf_proxy_network
    - default
    restart: always
    image: langgenius/dify-api:1.17.0
    environment:
      MODE: api
      DB_TYPE: postgresql
      DB_HOST: ${DIFY_DB_HOST}
      DB_PORT: '5432'
      DB_USERNAME: dify_user
      DB_PASSWORD: ${DIFY_DB_PASSWORD}
      DB_DATABASE: dify
      REDIS_HOST: ${DIFY_REDIS_HOST}
      REDIS_PORT: '6379'
      REDIS_USERNAME: default
      REDIS_PASSWORD: ${DIFY_REDIS_PASSWORD}
      REDIS_DB: '0'
      REDIS_USE_SSL: 'false'
      VECTOR_STORE: pgvector
      SANDBOX_API_KEY: ${NEXPLUZ_SANDBOX_API_KEY}
      SENTRY_DSN: ${API_SENTRY_DSN:-}
      SENTRY_TRACES_SAMPLE_RATE: ${API_SENTRY_TRACES_SAMPLE_RATE:-1.0}
      SENTRY_PROFILES_SAMPLE_RATE: ${API_SENTRY_PROFILES_SAMPLE_RATE:-1.0}
      PLUGIN_REMOTE_INSTALL_HOST: ${EXPOSE_PLUGIN_DEBUGGING_HOST:-localhost}
      PLUGIN_REMOTE_INSTALL_PORT: ${EXPOSE_PLUGIN_DEBUGGING_PORT:-5003}
      PLUGIN_MAX_PACKAGE_SIZE: ${PLUGIN_MAX_PACKAGE_SIZE:-52428800}
      PLUGIN_DAEMON_TIMEOUT: ${PLUGIN_DAEMON_TIMEOUT:-600.0}
      PLUGIN_DAEMON_KEY: ${NEXPLUZ_PLUGIN_DAEMON_KEY}
      INNER_API_KEY_FOR_PLUGIN: ${NEXPLUZ_PLUGIN_INNER_API_KEY}
      AGENT_BACKEND_BASE_URL: ${AGENT_BACKEND_BASE_URL:-http://agent_backend:5050}
      AGENT_BACKEND_API_TOKEN: ${NEXPLUZ_AGENT_API_TOKEN}
      PGVECTOR_HOST: ${DIFY_DB_HOST}
      PGVECTOR_PORT: '5432'
      PGVECTOR_USER: dify_user
      PGVECTOR_PASSWORD: ${DIFY_DB_PASSWORD}
      PGVECTOR_DATABASE: dify
      PGVECTOR_MIN_CONNECTION: '1'
      PGVECTOR_MAX_CONNECTION: '5'
    depends_on:
      init_permissions:
        condition: service_completed_successfully
      agent_backend:
        condition: service_started
    volumes:
    - ./volumes/app/storage:/app/api/storage
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:5001/health
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
  api_websocket:
    env_file:
    - path: ./envs/core-services/shared.env
      required: false
    - path: ./envs/core-services/api.env
      required: false
    - path: ./envs/security.env
      required: false
    - path: ./envs/databases/db-postgres.env
      required: false
    - path: ./envs/databases/redis.env
      required: false
    - ./.env
    networks:
    - ssrf_proxy_network
    - default
    restart: always
    image: langgenius/dify-api:1.17.0
    profiles:
    - collaboration
    environment:
      MODE: api
      DB_TYPE: postgresql
      DB_HOST: ${DIFY_DB_HOST}
      DB_PORT: '5432'
      DB_USERNAME: dify_user
      DB_PASSWORD: ${DIFY_DB_PASSWORD}
      DB_DATABASE: dify
      REDIS_HOST: ${DIFY_REDIS_HOST}
      REDIS_PORT: '6379'
      REDIS_USERNAME: default
      REDIS_PASSWORD: ${DIFY_REDIS_PASSWORD}
      REDIS_DB: '0'
      REDIS_USE_SSL: 'false'
      VECTOR_STORE: pgvector
      SERVER_WORKER_AMOUNT: ${API_WEBSOCKET_WORKER_AMOUNT:-1}
      SERVER_WORKER_CLASS: ${API_WEBSOCKET_WORKER_CLASS:-geventwebsocket.gunicorn.workers.GeventWebSocketWorker}
      SERVER_WORKER_CONNECTIONS: ${API_WEBSOCKET_WORKER_CONNECTIONS:-1000}
      GUNICORN_TIMEOUT: ${API_WEBSOCKET_GUNICORN_TIMEOUT:-360}
      PGVECTOR_HOST: ${DIFY_DB_HOST}
      PGVECTOR_PORT: '5432'
      PGVECTOR_USER: dify_user
      PGVECTOR_PASSWORD: ${DIFY_DB_PASSWORD}
      PGVECTOR_DATABASE: dify
      PGVECTOR_MIN_CONNECTION: '1'
      PGVECTOR_MAX_CONNECTION: '5'
    depends_on:
      init_permissions:
        condition: service_completed_successfully
    volumes:
    - ./volumes/app/storage:/app/api/storage
  worker:
    env_file:
    - path: ./envs/core-services/shared.env
      required: false
    - path: ./envs/core-services/worker.env
      required: false
    - path: ./envs/security.env
      required: false
    - path: ./envs/databases/db-postgres.env
      required: false
    - path: ./envs/databases/redis.env
      required: false
    - ./.env
    networks:
    - ssrf_proxy_network
    - default
    restart: always
    image: langgenius/dify-api:1.17.0
    environment:
      MODE: worker
      DB_TYPE: postgresql
      DB_HOST: ${DIFY_DB_HOST}
      DB_PORT: '5432'
      DB_USERNAME: dify_user
      DB_PASSWORD: ${DIFY_DB_PASSWORD}
      DB_DATABASE: dify
      REDIS_HOST: ${DIFY_REDIS_HOST}
      REDIS_PORT: '6379'
      REDIS_USERNAME: default
      REDIS_PASSWORD: ${DIFY_REDIS_PASSWORD}
      REDIS_DB: '0'
      REDIS_USE_SSL: 'false'
      VECTOR_STORE: pgvector
      SANDBOX_API_KEY: ${NEXPLUZ_SANDBOX_API_KEY}
      SENTRY_DSN: ${API_SENTRY_DSN:-}
      SENTRY_TRACES_SAMPLE_RATE: ${API_SENTRY_TRACES_SAMPLE_RATE:-1.0}
      SENTRY_PROFILES_SAMPLE_RATE: ${API_SENTRY_PROFILES_SAMPLE_RATE:-1.0}
      PLUGIN_MAX_PACKAGE_SIZE: ${PLUGIN_MAX_PACKAGE_SIZE:-52428800}
      INNER_API_KEY_FOR_PLUGIN: ${NEXPLUZ_PLUGIN_INNER_API_KEY}
      AGENT_BACKEND_BASE_URL: ${AGENT_BACKEND_BASE_URL:-http://agent_backend:5050}
      AGENT_BACKEND_API_TOKEN: ${NEXPLUZ_AGENT_API_TOKEN}
      PGVECTOR_HOST: ${DIFY_DB_HOST}
      PGVECTOR_PORT: '5432'
      PGVECTOR_USER: dify_user
      PGVECTOR_PASSWORD: ${DIFY_DB_PASSWORD}
      PGVECTOR_DATABASE: dify
      PGVECTOR_MIN_CONNECTION: '1'
      PGVECTOR_MAX_CONNECTION: '5'
    depends_on:
      init_permissions:
        condition: service_completed_successfully
      agent_backend:
        condition: service_started
    volumes:
    - ./volumes/app/storage:/app/api/storage
    healthcheck:
      test:
      - CMD-SHELL
      - celery -A celery_healthcheck.celery inspect ping
      interval: ${COMPOSE_WORKER_HEALTHCHECK_INTERVAL:-30s}
      timeout: ${COMPOSE_WORKER_HEALTHCHECK_TIMEOUT:-30s}
      retries: 3
      start_period: 60s
      disable: ${COMPOSE_WORKER_HEALTHCHECK_DISABLED:-true}
  worker_beat:
    env_file:
    - path: ./envs/core-services/shared.env
      required: false
    - path: ./envs/core-services/worker-beat.env
      required: false
    - path: ./envs/security.env
      required: false
    - path: ./envs/databases/db-postgres.env
      required: false
    - path: ./envs/databases/redis.env
      required: false
    - ./.env
    networks:
    - ssrf_proxy_network
    - default
    restart: always
    image: langgenius/dify-api:1.17.0
    environment:
      MODE: beat
      DB_TYPE: postgresql
      DB_HOST: ${DIFY_DB_HOST}
      DB_PORT: '5432'
      DB_USERNAME: dify_user
      DB_PASSWORD: ${DIFY_DB_PASSWORD}
      DB_DATABASE: dify
      REDIS_HOST: ${DIFY_REDIS_HOST}
      REDIS_PORT: '6379'
      REDIS_USERNAME: default
      REDIS_PASSWORD: ${DIFY_REDIS_PASSWORD}
      REDIS_DB: '0'
      REDIS_USE_SSL: 'false'
      VECTOR_STORE: pgvector
      PGVECTOR_HOST: ${DIFY_DB_HOST}
      PGVECTOR_PORT: '5432'
      PGVECTOR_USER: dify_user
      PGVECTOR_PASSWORD: ${DIFY_DB_PASSWORD}
      PGVECTOR_DATABASE: dify
      PGVECTOR_MIN_CONNECTION: '1'
      PGVECTOR_MAX_CONNECTION: '5'
    depends_on:
      init_permissions:
        condition: service_completed_successfully
    healthcheck:
      test:
      - CMD-SHELL
      - celery -A celery_healthcheck.celery inspect ping
      interval: ${COMPOSE_WORKER_HEALTHCHECK_INTERVAL:-30s}
      timeout: ${COMPOSE_WORKER_HEALTHCHECK_TIMEOUT:-30s}
      retries: 3
      start_period: 60s
      disable: ${COMPOSE_WORKER_HEALTHCHECK_DISABLED:-true}
  web:
    image: langgenius/dify-web:1.17.0
    restart: always
    env_file:
    - path: ./envs/core-services/web.env
      required: false
    - path: ./envs/security.env
      required: false
    - ./.env
    environment:
      CONSOLE_API_URL: ${CONSOLE_API_URL:-}
      SERVER_CONSOLE_API_URL: ${SERVER_CONSOLE_API_URL:-http://api:5001}
      APP_API_URL: ${APP_API_URL:-}
      AMPLITUDE_API_KEY: ${AMPLITUDE_API_KEY:-}
      TURNSTILE_SITE_KEY: ${TURNSTILE_SITE_KEY:-}
      NEXT_PUBLIC_COOKIE_DOMAIN: ${NEXT_PUBLIC_COOKIE_DOMAIN:-}
      NEXT_PUBLIC_SOCKET_URL: ${NEXT_PUBLIC_SOCKET_URL:-ws://localhost}
      SENTRY_DSN: ${WEB_SENTRY_DSN:-}
      NEXT_TELEMETRY_DISABLED: ${NEXT_TELEMETRY_DISABLED:-0}
      EXPERIMENTAL_ENABLE_VINEXT: ${EXPERIMENTAL_ENABLE_VINEXT:-false}
      TEXT_GENERATION_TIMEOUT_MS: ${TEXT_GENERATION_TIMEOUT_MS:-60000}
      CSP_WHITELIST: ${CSP_WHITELIST:-}
      ALLOW_EMBED: ${ALLOW_EMBED:-false}
      ALLOW_UNSAFE_DATA_SCHEME: ${ALLOW_UNSAFE_DATA_SCHEME:-false}
      MARKETPLACE_API_URL: ${MARKETPLACE_API_URL:-https://marketplace.dify.ai}
      MARKETPLACE_URL: ${MARKETPLACE_URL:-https://marketplace.dify.ai}
      TOP_K_MAX_VALUE: ${TOP_K_MAX_VALUE:-10}
      INDEXING_MAX_SEGMENTATION_TOKENS_LENGTH: ${INDEXING_MAX_SEGMENTATION_TOKENS_LENGTH:-4000}
      LOOP_NODE_MAX_COUNT: ${LOOP_NODE_MAX_COUNT:-100}
      MAX_TOOLS_NUM: ${MAX_TOOLS_NUM:-10}
      MAX_PARALLEL_LIMIT: ${MAX_PARALLEL_LIMIT:-10}
      MAX_ITERATIONS_NUM: ${MAX_ITERATIONS_NUM:-99}
      MAX_TREE_DEPTH: ${MAX_TREE_DEPTH:-50}
      ENABLE_WEBSITE_JINAREADER: ${ENABLE_WEBSITE_JINAREADER:-true}
      ENABLE_WEBSITE_FIRECRAWL: ${ENABLE_WEBSITE_FIRECRAWL:-true}
      ENABLE_WEBSITE_WATERCRAWL: ${ENABLE_WEBSITE_WATERCRAWL:-true}
  sandbox:
    image: langgenius/dify-sandbox:0.2.15
    restart: always
    env_file:
    - path: ./envs/core-services/sandbox.env
      required: false
    - path: ./envs/security.env
      required: false
    - ./.env
    environment:
      API_KEY: ${NEXPLUZ_SANDBOX_API_KEY}
      GIN_MODE: ${SANDBOX_GIN_MODE:-release}
      WORKER_TIMEOUT: ${SANDBOX_WORKER_TIMEOUT:-15}
      ENABLE_NETWORK: ${SANDBOX_ENABLE_NETWORK:-true}
      HTTP_PROXY: ${SANDBOX_HTTP_PROXY:-http://ssrf_proxy:3128}
      HTTPS_PROXY: ${SANDBOX_HTTPS_PROXY:-http://ssrf_proxy:3128}
      SANDBOX_PORT: ${SANDBOX_PORT:-8194}
      PIP_MIRROR_URL: ${PIP_MIRROR_URL:-}
    volumes:
    - ./volumes/sandbox/dependencies:/dependencies
    - ./volumes/sandbox/conf:/conf
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8194/health
    networks:
    - ssrf_proxy_network
  local_sandbox:
    image: langgenius/dify-agent-local-sandbox:1.17.0
    restart: always
    env_file:
    - path: ./envs/core-services/local-sandbox.env
      required: false
    environment:
    - SHELLCTL_AUTH_TOKEN=${NEXPLUZ_LOCAL_SANDBOX_AUTH_TOKEN}
    - HTTP_PROXY=http://agent_ssrf_proxy:3128
    - HTTPS_PROXY=http://agent_ssrf_proxy:3128
    - NO_PROXY=localhost,127.0.0.1
    volumes:
    - dify_agent_local_sandbox_home:/home/dify
    - dify_agent_local_sandbox_workspace:/workspace
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:5004/healthz
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
    - agent_sandbox_network
    - local_sandbox_proxy_network
  plugin_daemon:
    image: langgenius/dify-plugin-daemon:0.6.10-local
    restart: always
    env_file:
    - path: ./envs/core-services/shared.env
      required: false
    - path: ./envs/core-services/plugin-daemon.env
      required: false
    - path: ./envs/security.env
      required: false
    - path: ./envs/databases/db-postgres.env
      required: false
    - path: ./envs/databases/redis.env
      required: false
    - ./.env
    networks:
    - ssrf_proxy_network
    - default
    environment:
      DB_TYPE: postgresql
      DB_HOST: ${DIFY_DB_HOST}
      DB_PORT: '5432'
      DB_USERNAME: dify_user
      DB_PASSWORD: ${DIFY_DB_PASSWORD}
      DB_DATABASE: dify_plugin
      REDIS_HOST: ${DIFY_REDIS_HOST}
      REDIS_PORT: '6379'
      REDIS_USERNAME: default
      REDIS_PASSWORD: ${DIFY_REDIS_PASSWORD}
      REDIS_DB: '0'
      REDIS_USE_SSL: 'false'
      DB_SSL_MODE: ${DB_SSL_MODE:-disable}
      SERVER_PORT: ${PLUGIN_DAEMON_PORT:-5002}
      SERVER_KEY: ${NEXPLUZ_PLUGIN_DAEMON_KEY}
      MAX_PLUGIN_PACKAGE_SIZE: ${PLUGIN_MAX_PACKAGE_SIZE:-52428800}
      PPROF_ENABLED: ${PLUGIN_PPROF_ENABLED:-false}
      DIFY_INNER_API_URL: ${PLUGIN_DIFY_INNER_API_URL:-http://api:5001}
      DIFY_INNER_API_KEY: ${NEXPLUZ_PLUGIN_INNER_API_KEY}
      PLUGIN_REMOTE_INSTALLING_HOST: ${PLUGIN_DEBUGGING_HOST:-0.0.0.0}
      PLUGIN_REMOTE_INSTALLING_PORT: ${PLUGIN_DEBUGGING_PORT:-5003}
      PLUGIN_WORKING_PATH: ${PLUGIN_WORKING_PATH:-/app/storage/cwd}
      FORCE_VERIFYING_SIGNATURE: ${FORCE_VERIFYING_SIGNATURE:-true}
      PYTHON_ENV_INIT_TIMEOUT: ${PLUGIN_PYTHON_ENV_INIT_TIMEOUT:-120}
      PLUGIN_MAX_EXECUTION_TIMEOUT: ${PLUGIN_MAX_EXECUTION_TIMEOUT:-600}
      PLUGIN_STDIO_BUFFER_SIZE: ${PLUGIN_STDIO_BUFFER_SIZE:-1024}
      PLUGIN_STDIO_MAX_BUFFER_SIZE: ${PLUGIN_STDIO_MAX_BUFFER_SIZE:-5242880}
      PIP_MIRROR_URL: ${PIP_MIRROR_URL:-}
      PLUGIN_STORAGE_TYPE: ${PLUGIN_STORAGE_TYPE:-local}
      PLUGIN_STORAGE_LOCAL_ROOT: ${PLUGIN_STORAGE_LOCAL_ROOT:-/app/storage}
      PLUGIN_INSTALLED_PATH: ${PLUGIN_INSTALLED_PATH:-plugin}
      PLUGIN_PACKAGE_CACHE_PATH: ${PLUGIN_PACKAGE_CACHE_PATH:-plugin_packages}
      PLUGIN_MEDIA_CACHE_PATH: ${PLUGIN_MEDIA_CACHE_PATH:-assets}
      PLUGIN_STORAGE_OSS_BUCKET: ${PLUGIN_STORAGE_OSS_BUCKET:-}
      S3_USE_AWS_MANAGED_IAM: ${PLUGIN_S3_USE_AWS_MANAGED_IAM:-false}
      S3_USE_AWS: ${PLUGIN_S3_USE_AWS:-false}
      S3_ENDPOINT: ${PLUGIN_S3_ENDPOINT:-}
      S3_USE_PATH_STYLE: ${PLUGIN_S3_USE_PATH_STYLE:-false}
      AWS_ACCESS_KEY: ${PLUGIN_AWS_ACCESS_KEY:-}
      AWS_SECRET_KEY: ${PLUGIN_AWS_SECRET_KEY:-}
      AWS_REGION: ${PLUGIN_AWS_REGION:-}
      AZURE_BLOB_STORAGE_CONNECTION_STRING: ${PLUGIN_AZURE_BLOB_STORAGE_CONNECTION_STRING:-}
      AZURE_BLOB_STORAGE_CONTAINER_NAME: ${PLUGIN_AZURE_BLOB_STORAGE_CONTAINER_NAME:-}
      TENCENT_COS_SECRET_KEY: ${PLUGIN_TENCENT_COS_SECRET_KEY:-}
      TENCENT_COS_SECRET_ID: ${PLUGIN_TENCENT_COS_SECRET_ID:-}
      TENCENT_COS_REGION: ${PLUGIN_TENCENT_COS_REGION:-}
      ALIYUN_OSS_REGION: ${PLUGIN_ALIYUN_OSS_REGION:-}
      ALIYUN_OSS_ENDPOINT: ${PLUGIN_ALIYUN_OSS_ENDPOINT:-}
      ALIYUN_OSS_ACCESS_KEY_ID: ${PLUGIN_ALIYUN_OSS_ACCESS_KEY_ID:-}
      ALIYUN_OSS_ACCESS_KEY_SECRET: ${PLUGIN_ALIYUN_OSS_ACCESS_KEY_SECRET:-}
      ALIYUN_OSS_AUTH_VERSION: ${PLUGIN_ALIYUN_OSS_AUTH_VERSION:-v4}
      ALIYUN_OSS_PATH: ${PLUGIN_ALIYUN_OSS_PATH:-}
      VOLCENGINE_TOS_ENDPOINT: ${PLUGIN_VOLCENGINE_TOS_ENDPOINT:-}
      VOLCENGINE_TOS_ACCESS_KEY: ${PLUGIN_VOLCENGINE_TOS_ACCESS_KEY:-}
      VOLCENGINE_TOS_SECRET_KEY: ${PLUGIN_VOLCENGINE_TOS_SECRET_KEY:-}
      VOLCENGINE_TOS_REGION: ${PLUGIN_VOLCENGINE_TOS_REGION:-}
      SENTRY_ENABLED: ${PLUGIN_SENTRY_ENABLED:-false}
      SENTRY_DSN: ${PLUGIN_SENTRY_DSN:-}
    volumes:
    - ./volumes/plugin_daemon:/app/storage
  agent_backend:
    image: langgenius/dify-agent-backend:1.17.0
    restart: always
    env_file:
    - path: ./envs/core-services/dify-agent.env
      required: false
    - path: ./envs/security.env
      required: false
    - path: ./envs/databases/redis.env
      required: false
    - ./.env
    environment:
      DIFY_AGENT_REDIS_URL: redis://default:${DIFY_REDIS_PASSWORD}@${DIFY_REDIS_HOST}:6379/2
      DIFY_AGENT_PLUGIN_DAEMON_URL: ${DIFY_AGENT_PLUGIN_DAEMON_URL:-${PLUGIN_DAEMON_URL:-http://plugin_daemon:5002}}
      DIFY_AGENT_PLUGIN_DAEMON_API_KEY: ${NEXPLUZ_PLUGIN_DAEMON_KEY}
      DIFY_AGENT_INNER_API_URL: ${DIFY_AGENT_INNER_API_URL:-${PLUGIN_DIFY_INNER_API_URL:-http://api:5001}}
      DIFY_AGENT_INNER_API_KEY: ${NEXPLUZ_PLUGIN_INNER_API_KEY}
      DIFY_AGENT_RUNTIME_BACKEND: ${DIFY_AGENT_RUNTIME_BACKEND:-local}
      DIFY_AGENT_LOCAL_SANDBOX_ENDPOINT: ${DIFY_AGENT_LOCAL_SANDBOX_ENDPOINT:-${DIFY_AGENT_SHELLCTL_ENTRYPOINT:-http://local_sandbox:5004}}
      DIFY_AGENT_LOCAL_SANDBOX_AUTH_TOKEN: ${NEXPLUZ_LOCAL_SANDBOX_AUTH_TOKEN}
      DIFY_AGENT_STUB_API_BASE_URL: ${DIFY_AGENT_STUB_API_BASE_URL:-http://agent_backend:5050/agent-stub}
      DIFY_AGENT_SANDBOX_FILES_BASE_URL: ${DIFY_AGENT_SANDBOX_FILES_BASE_URL:-http://api:5001}
      DIFY_AGENT_SERVER_SECRET_KEY: ${NEXPLUZ_AGENT_SERVER_SECRET}
      DIFY_AGENT_API_TOKEN: ${NEXPLUZ_AGENT_API_TOKEN}
    depends_on:
      plugin_daemon:
        condition: service_started
    networks:
    - default
    - agent_sandbox_network
  agent_ssrf_proxy:
    image: ubuntu/squid:latest
    restart: always
    volumes:
    - ./ssrf_proxy/squid-agent.conf.template:/etc/squid/squid.conf.template
    - ./ssrf_proxy/squid-common.conf.template:/etc/squid/dify_common.conf.template
    - ./ssrf_proxy/docker-agent-entrypoint.sh:/docker-entrypoint-mount.sh
    entrypoint:
    - sh
    - -c
    - "cp /docker-entrypoint-mount.sh /docker-entrypoint.sh && sed -i 's/\r$$//' /docker-entrypoint.sh && chmod +x /docker-entrypoint.sh\
      \ && /docker-entrypoint.sh"
    environment:
      HTTP_PORT: ${SSRF_HTTP_PORT:-3128}
      COREDUMP_DIR: ${SSRF_COREDUMP_DIR:-/var/spool/squid}
    networks:
    - default
    - local_sandbox_proxy_network
  ssrf_proxy:
    image: ubuntu/squid:latest
    restart: always
    volumes:
    - ./ssrf_proxy/squid.conf.template:/etc/squid/squid.conf.template
    - ./ssrf_proxy/squid-common.conf.template:/etc/squid/dify_common.conf.template
    - ./ssrf_proxy/docker-entrypoint.sh:/docker-entrypoint-mount.sh
    entrypoint:
    - sh
    - -c
    - "cp /docker-entrypoint-mount.sh /docker-entrypoint.sh && sed -i 's/\r$$//' /docker-entrypoint.sh && chmod +x /docker-entrypoint.sh\
      \ && /docker-entrypoint.sh"
    environment:
      HTTP_PORT: ${SSRF_HTTP_PORT:-3128}
      COREDUMP_DIR: ${SSRF_COREDUMP_DIR:-/var/spool/squid}
      SSRF_PROXY_ALLOW_PRIVATE_IPS: ${SSRF_PROXY_ALLOW_PRIVATE_IPS:-}
      SSRF_PROXY_ALLOW_PRIVATE_DOMAINS: ${SSRF_PROXY_ALLOW_PRIVATE_DOMAINS:-}
    networks:
    - ssrf_proxy_network
    - default
  nginx:
    image: nginx:latest
    restart: always
    volumes:
    - ./nginx/nginx.conf.template:/etc/nginx/nginx.conf.template
    - ./nginx/proxy.conf.template:/etc/nginx/proxy.conf.template
    - ./nginx/https.conf.template:/etc/nginx/https.conf.template
    - ./nginx/conf.d:/etc/nginx/conf.d
    - ./nginx/docker-entrypoint.sh:/docker-entrypoint-mount.sh
    - ./nginx/ssl:/etc/ssl
    - ./volumes/certbot/conf/live:/etc/letsencrypt/live
    - ./volumes/certbot/conf:/etc/letsencrypt
    - ./volumes/certbot/www:/var/www/html
    entrypoint:
    - sh
    - -c
    - "cp /docker-entrypoint-mount.sh /docker-entrypoint.sh && sed -i 's/\r$$//' /docker-entrypoint.sh && chmod +x /docker-entrypoint.sh\
      \ && /docker-entrypoint.sh"
    environment:
      SERVICE_FQDN_NGINX: ${SERVICE_FQDN_NGINX}
      
      NGINX_SERVER_NAME: ${NGINX_SERVER_NAME:-_}
      NGINX_HTTPS_ENABLED: ${NGINX_HTTPS_ENABLED:-false}
      NGINX_SSL_PORT: ${NGINX_SSL_PORT:-443}
      NGINX_PORT: ${NGINX_PORT:-80}
      NGINX_SSL_CERT_FILENAME: ${NGINX_SSL_CERT_FILENAME:-dify.crt}
      NGINX_SSL_CERT_KEY_FILENAME: ${NGINX_SSL_CERT_KEY_FILENAME:-dify.key}
      NGINX_SSL_PROTOCOLS: ${NGINX_SSL_PROTOCOLS:-TLSv1.2 TLSv1.3}
      NGINX_WORKER_PROCESSES: ${NGINX_WORKER_PROCESSES:-auto}
      NGINX_CLIENT_MAX_BODY_SIZE: ${NGINX_CLIENT_MAX_BODY_SIZE:-100M}
      NGINX_KEEPALIVE_TIMEOUT: ${NGINX_KEEPALIVE_TIMEOUT:-65}
      NGINX_PROXY_READ_TIMEOUT: ${NGINX_PROXY_READ_TIMEOUT:-3600s}
      NGINX_PROXY_SEND_TIMEOUT: ${NGINX_PROXY_SEND_TIMEOUT:-3600s}
      NGINX_ENABLE_CERTBOT_CHALLENGE: ${NGINX_ENABLE_CERTBOT_CHALLENGE:-false}
      NGINX_SOCKET_IO_UPSTREAM: ${NGINX_SOCKET_IO_UPSTREAM:-api:5001}
      CERTBOT_DOMAIN: ${CERTBOT_DOMAIN:-}
    depends_on:
    - api
    - web
    expose:
    - '80'
    networks:
    - default
networks:
  ssrf_proxy_network:
    driver: bridge
    internal: true
  local_sandbox_proxy_network:
    driver: bridge
    internal: true
  agent_sandbox_network:
    driver: bridge
    internal: true
volumes:
  dify_agent_local_sandbox_home: null
  dify_agent_local_sandbox_workspace: null
